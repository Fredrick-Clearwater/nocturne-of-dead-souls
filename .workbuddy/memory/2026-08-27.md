# 2026-08-27 工作日志

- **GitHub Pages 部署修复（跨日收尾）**：`nocturne-of-dead-souls` 首次 Pages 构建卡在 `building` 约 50 分钟，接口最终翻成 `errored`（无具体报错，典型哑错）。
  - 根因：GitHub 对纯静态 HTML 仍默认跑 Jekyll 编译，遇特殊结构/字符易翻车。
  - 修法：仓库根目录加 `.nojekyll`（空文件）跳过 Jekyll → `git commit` → `git push` → `gh api POST /pages/builds` 重新触发 → 约 2 分钟变 `built`。
  - 最终状态：**已上线** `https://fredrick-clearwater.github.io/nocturne-of-dead-souls/`（HTTP 200，标题正确）。
  - 复用提示：今后任何静态 HTML 站点走 GitHub Pages 分支部署，直接带 `.nojekyll` 可避免此坑。

- **角色再平衡（试玩反馈）**：用户试玩后认为 降临者太弱（只能验死神）、米提亚太慢（一轮验1+魂灵少概率低）、莱莱可作用不大。落地两项修改（已同步 V4.2/V4.3 两份规则书 + 分享页 HTML + 推送 Pages）：
  - **米提亚加强**：每夜查 2 人，结果「都是生者」(皆非狼) 或「魂灵在它们之中」(≥1狼，不指人数/是谁)；新增「验己保险法」= 验〔自己+X〕可绝对确认 X 身份（代价：暴露查验方式易被狼刀）。
  - **降临者加强**：每夜查死神 + 每个白天 1 次「指认死神」机会（投票前声明）；命中则强迫死神当场自爆进入黑夜（全场知其身份、次日回归、跳过其一夜、当日投票作废），猜错空过。相当于每轮两次触及死神。
  - **莱莱可**：用户未定如何加强，待给方案（见对话：建议①刺客刀最高优先/豁免上限确保必中；②自爆改为同归于尽带走1人；③白天决斗赌命；④N1探针）。
  - 平衡预警：V4.3（含夜祷掩护）信息差仍可对冲；V4.2（无夜祷）加强后可能偏人方，建议以 V4.3 为基准试玩。
