# 2026-08-27 工作日志

- **GitHub Pages 部署修复（跨日收尾）**：`nocturne-of-dead-souls` 首次 Pages 构建卡在 `building` 约 50 分钟，接口最终翻成 `errored`（无具体报错，典型哑错）。
  - 根因：GitHub 对纯静态 HTML 仍默认跑 Jekyll 编译，遇特殊结构/字符易翻车。
  - 修法：仓库根目录加 `.nojekyll`（空文件）跳过 Jekyll → `git commit` → `git push` → `gh api POST /pages/builds` 重新触发 → 约 2 分钟变 `built`。
  - 最终状态：**已上线** `https://fredrick-clearwater.github.io/nocturne-of-dead-souls/`（HTTP 200，标题正确）。
  - 复用提示：今后任何静态 HTML 站点走 GitHub Pages 分支部署，直接带 `.nojekyll` 可避免此坑。
